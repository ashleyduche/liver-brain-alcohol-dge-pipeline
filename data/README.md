# data/

- `example/` — synthetic demo dataset (committed, used by default).
- `raw/` — real study data goes here once you have it locally. Gitignored; never committed.
- `processed/` — intermediate files the pipeline generates on each run (e.g. parsed sample metadata). Gitignored; regenerated automatically.
